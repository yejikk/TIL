* 스키마
  * 작성자는 공백일 수 없다 
  * 열마다 고유한 데이터 형식이 있다.  (속성에 관한 값)
  * 기본키는 중복될 수 없다. (고유한 값을 가져야지 접근이 가능하기 때문에)

* SELECT * FROM example

  (키워드  모든것 example로 부터)

  (SELECT  =>  print와 같은 역할)

* DROP TABLE  classmates : 삭제

* SELE